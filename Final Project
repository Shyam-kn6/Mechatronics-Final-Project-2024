“““
import RPi.GPIO as GPIO
import time
import lcd
import sys
GPIO.setmode(GPIO.BCM)
GPIO.setwarnings(False)
mode=GPIO.getmode()
from gpiozero import DistanceSensor

GPIO.setup(21, GPIO.OUT)
servo = GPIO.PWM(21,50)

ultrasonic = DistanceSensor(echo=24, trigger=23)

motor1_forward=5
motor2_forward=7
motor1_backward=0
motor2_backward=0
motor1_backward=0
motor2_backward=6

status_LED1 = 19
status_LED2 = 26
status_LED3 = 12
buzzer_pin =16
PWMLED = 19

out1 = 17
out2 = 22
out3 = 27
out4 = 10
# Define the time between each step (in seconds) and the number of steps.
step_sleep=0.02
step_count=100
# Setting up
GPIO.setmode(GPIO.BCM)
GPIO.setup(out1,GPIO.OUT)
GPIO.setup(out2,GPIO.OUT)
GPIO.setup(out3,GPIO.OUT)
GPIO.setup(out4,GPIO.OUT)
# Initializing
GPIO.output(out1,GPIO.LOW)
GPIO.output(out2,GPIO.LOW)
GPIO.output(out3,GPIO.LOW)
GPIO.output(out4,GPIO.LOW)

GPIO.setup(motor1_forward, GPIO.OUT)
GPIO.setup(motor2_forward, GPIO.OUT)
GPIO.setup(motor1_backward, GPIO.OUT)
GPIO.setup(motor2_backward, GPIO.OUT)

GPIO.setup(status_LED1, GPIO.OUT)
GPIO.setup(status_LED2, GPIO.OUT)
GPIO.setup(status_LED3, GPIO.OUT)
GPIO.setup(PWMLED, GPIO.OUT)

GPIO.setup(buzzer_pin, GPIO.OUT)
pin1 = GPIO.PWM(buzzer_pin,500)

# Turn ON the status LED
GPIO.output(status_LED1,GPIO.HIGH)
# Keep the LED on for 2 seconds 

try:
     #Setup LCD
 lcd.lcd_init()
 time.sleep(1)
    
 #Print Hello to LCD then clear it
 lcd.printer("Hello","")
 print("hello ")
 time.sleep(1)
 lcd.printer("","")
    

    
except KeyboardInterrupt:
    pass

pin1.start(40)
time.sleep(2)
pin1.stop()

try:
     #Setup LCD
 lcd.lcd_init()
 time.sleep(1)
    
 lcd.printer("Moving Forward","")
 time.sleep(1)
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass


# Activate forward pins
GPIO.output(motor1_forward, GPIO.HIGH)
GPIO.output(motor2_forward, GPIO.HIGH)

GPIO.output(status_LED2,GPIO.HIGH)
GPIO.output(status_LED3,GPIO.HIGH)
print("Moving Forward")
time.sleep(4)
# Turn off the motors after moving
GPIO.output(motor1_forward, GPIO.LOW)
GPIO.output(motor2_forward, GPIO.LOW)


dist = ultrasonic.distance
strdist = str(dist)
servo.start(0)

try:
    
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 if (dist < 0.1):
  pin1.start(40)
  time.sleep(2)
  pin1.stop()
 
 lcd.printer(strdist,"")
 time.sleep(0.5)
 servo.ChangeDutyCycle(10)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 servo.ChangeDutyCycle(6)
 
 try:
    i = 0
    for i in range (step_count):
        # This pattern energizes the motor coils in sequence to make it step.
            if i%4 ==0:
                GPIO.output(out4,GPIO.HIGH)
                GPIO.output(out3,GPIO.LOW)
                GPIO.output(out2,GPIO.HIGH)
                GPIO.output(out1,GPIO.LOW)
                
            elif i%4 ==1:
                GPIO.output(out4,GPIO.LOW)
                GPIO.output(out3,GPIO.LOW)
                GPIO.output(out2,GPIO.HIGH)
                GPIO.output(out1,GPIO.HIGH)
            elif i%4 ==2:
                GPIO.output(out4,GPIO.LOW)
                GPIO.output(out3,GPIO.HIGH)
                GPIO.output(out2,GPIO.LOW)
                GPIO.output(out1,GPIO.HIGH)
            elif i%4 ==3:
                GPIO.output(out4,GPIO.HIGH)
                GPIO.output(out3,GPIO.HIGH)
                GPIO.output(out2,GPIO.LOW)
                GPIO.output(out1,GPIO.LOW)
            
            time.sleep(step_sleep)
        
 except KeyboardInterrupt:
    pass
 
 if (dist < 0.1):
  pin1.start(40)
  time.sleep(2)
  pin1.stop()
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 if (dist < 0.1):
  pin1.start(40)
  time.sleep(2)
  pin1.stop()
 
 lcd.printer(strdist,"")
 dist = ultrasonic.distance
 strdist = str(dist)
 time.sleep(0.5)
 
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass

try:
     #Setup LCD
    
 lcd.printer("Pause","")
 servo.stop()
 GPIO.output(status_LED2,GPIO.LOW)
 GPIO.output(status_LED3,GPIO.LOW)
 GPIO.output(status_LED1,GPIO.LOW)
 time.sleep(4)
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass


pin1 = GPIO.PWM(buzzer_pin,1000)
pin1.start(40)
time.sleep(2)
pin1.stop()

pin2 = GPIO.PWM(PWMLED,300)
duty_cycle = 100

try:
    
 lcd.printer("Moving Backward","")
 pin2.start(duty_cycle)
 
 GPIO.output(motor1_backward, GPIO.HIGH)
 GPIO.output(motor2_backward, GPIO.HIGH)
 
 time.sleep(4)
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass

GPIO.output(motor1_backward, GPIO.LOW)
GPIO.output(motor2_backward, GPIO.LOW)
pin2.stop(duty_cycle)
GPIO.output(status_LED1,GPIO.LOW)

try:
    
 lcd.printer("Turning right","")
 
 GPIO.output(motor1_forward, GPIO.HIGH)
 GPIO.output(motor2_backward, GPIO.HIGH)
 
 time.sleep(1)
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass

GPIO.output(motor1_forward, GPIO.LOW)
GPIO.output(motor2_backward, GPIO.LOW)

try:
    
 lcd.printer("Turning left","")
 
 GPIO.output(motor1_backward, GPIO.HIGH)
 GPIO.output(motor2_forward, GPIO.HIGH)
 
 time.sleep(1)
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass

GPIO.output(motor1_backward, GPIO.LOW)
GPIO.output(motor2_forward, GPIO.LOW)



GPIO.setmode(GPIO.BCM)
GPIO.setwarnings(False)
# Initializing PWM, the frequency of the PWM signal in Hertz.
pin1 = GPIO.PWM(status_LED1,500)
pin2 = GPIO.PWM(status_LED2,500)
pin3 = GPIO.PWM(status_LED3,500)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.start(50)
pin2.start(50)
pin3.start(50)

time.sleep(0.5)

pin1.start(100)
pin2.start(100)
pin3.start(100)

time.sleep(0.5)

pin1.stop() # Stop the PWM
GPIO.cleanup()



try:
    
 lcd.printer("Goodbye","")
 print("bye")
 time.sleep(2)
 lcd.printer("","")
    
except KeyboardInterrupt:
    pass


GPIO.cleanup()

”””
